"""Scrap: search the web for images on a topic, dedupe, label by hand,
and emit a YOLO dataset + scenario profile that the existing trainer
([mlops.pipeline.train.run_training]) can consume directly.
"""
